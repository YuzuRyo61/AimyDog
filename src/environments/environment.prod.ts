export const environment = {
  // eslint-disable-next-line @typescript-eslint/no-var-requires
  appVersion: require('../../package.json').version,
  // eslint-disable-next-line @typescript-eslint/no-var-requires
  repositoryUrl: require('../../package.json').repository.url,
  production: true
};
